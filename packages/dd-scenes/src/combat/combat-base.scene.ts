import { Settings } from "dd-config";
import { BaseScene } from "../base.scene";

export class CombatBaseScene extends BaseScene {


	constructor(key: string, settings: Settings) {
		super(key, settings);
	}

}