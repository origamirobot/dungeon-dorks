import { Settings } from "dd-config";
import { BaseScene } from ".";

export class ContinueScene extends BaseScene {


	constructor(settings: Settings) {
		super('CONTINUE', settings);
	}

}